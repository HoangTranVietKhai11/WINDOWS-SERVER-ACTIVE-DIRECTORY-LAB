# Screenshots Directory

Thư mục này chứa các hình ảnh minh chứng kết quả triển khai thực tế cho từng giai đoạn của lab:

- `01-ad-ds-installed.png`: Cửa sổ Server Manager và Active Directory Users and Computers sau khi promote Domain Controller (`corp.local`).
- `02-ou-structure.png`: Cấu trúc Organizational Unit (OU: `CORP > IT, HR, Sales, Computers`) và Security Groups trong ADUC.
- `04-gpo-cmd-blocked.png`: Màn hình kiểm thử trên Client khi Command Prompt, USB, và Control Panel bị chặn bởi GPO.
- `06-dhcp-client-ip.png`: Kết quả lệnh `ipconfig /all` trên máy Client nhận IP tự động từ DHCP Scope (`192.168.101.x`).
- `07-file-permissions.png`: Cửa sổ phân quyền NTFS & Share Permissions theo mô hình AGDLP (`CompanyData`).
- `09-tailscale-vpn-connected.png`: Giao diện Admin Console của Tailscale và kết quả ping/RDP từ xa thành công.

*Hướng dẫn: Thay thế các file ảnh trên bằng ảnh chụp màn hình kết quả lab thực tế của bạn.*
